# Email Configuration for WhatsApp API Failure Notifications
# Copy this to your .env file and configure with your email settings

# SMTP Configuration (Example for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Alternative SMTP settings for other providers:
# For Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# For Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587

# Default sender email (usually same as SMTP_USER)
EMAIL_FROM=your-email@gmail.com

# Enable/disable email notifications (set to 'false' to disable)
EMAIL_NOTIFICATIONS_ENABLED=true

# Note: For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" instead of using your regular password
# 3. Use the app password as SMTP_PASS

# Google reCAPTCHA v3 Configuration
# Get your keys from: https://www.google.com/recaptcha/admin
RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here

# Note: For reCAPTCHA v3:
# 1. Register your domain at https://www.google.com/recaptcha/admin
# 2. Select reCAPTCHA v3
# 3. Copy the site key (for frontend) and secret key (for backend)
# 4. The default minimum score threshold is 0.5 (can be adjusted in middleware)